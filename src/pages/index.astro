---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const aktueltPosts = (await getCollection('aktuelt')).sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
).slice(0, 3);

const portfolioItems = (await getCollection('portfolio')).sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
).slice(0, 3);
---

<BaseLayout title="Hjem">
  <!-- Hero -->
  <section class="hero">
    <div class="container">
      <h1>Ordbruket</h1>
      <p>
        Trenger du språkvask, tekstproduksjon eller oversettelse? Da er du på rett sted!
      </p>
      <p>
        Har du behov for en sak til magasinet ditt? Trenger du hjelp med teksten din?
        Synes du det er et ork å lage poster til sosiale medier? Jeg fikser det!
      </p>
      <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;">
        <a href="/spraaktjenester/" class="btn">Se tjenester</a>
        <a href="/kontakt/" class="btn btn-outline">Ta kontakt</a>
      </div>
    </div>
  </section>

  <!-- Services overview -->
  <section class="section">
    <div class="container">
      <div class="section-title">
        <h2>Kort sagt en fullblods frilansjournalist og oversetter</h2>
        <p>Over 30 års erfaring som journalist og rådgiver innen kommunikasjon</p>
      </div>
      <div class="services-grid">
        <div class="service-item">
          <div class="service-icon"><svg viewBox="0 0 24 24"><path d="M17 3a2.83 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg></div>
          <h3>Tekstproduksjon</h3>
          <p>Artikler, reportasjer, portrettintervjuer og mer. Ordbruket leverer det du trenger av tekst.</p>
        </div>
        <div class="service-item">
          <div class="service-icon"><svg viewBox="0 0 24 24"><path d="M4 6h16M4 12h16M4 18h10"/></svg></div>
          <h3>Språkvask og korrektur</h3>
          <p>Språkvask, redigering, korrektur. Velg mellom 40 graders vask og en kraftfull kokvask på 90 grader.</p>
        </div>
        <div class="service-item">
          <div class="service-icon"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><path d="M2 12h20M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg></div>
          <h3>Oversettelse</h3>
          <p>Engelsk til norsk, nynorsk til bokmål og omvendt, svensk og dansk til norsk.</p>
        </div>
      </div>
      <div style="text-align: center; margin-top: 2rem;">
        <a href="/spraaktjenester/" class="btn btn-outline">Se alle tjenester</a>
      </div>
    </div>
  </section>

  <!-- Latest Activity -->
  {aktueltPosts.length > 0 && (
    <section class="section section-alt">
      <div class="container">
        <div class="section-title">
          <h2>Aktuelt</h2>
          <p>Siste nytt fra Ordbruket</p>
        </div>
        <div class="activity-feed">
          {aktueltPosts.map((post) => (
            <article class="activity-post">
              <div class="activity-post-header">
                <div class="activity-avatar">K</div>
                <div class="activity-post-meta">
                  <strong>Kati Foss Indrefjord</strong><br />
                  {new Date(post.data.date).toLocaleDateString('nb-NO', {
                    day: 'numeric',
                    month: 'long',
                    year: 'numeric',
                  })}
                </div>
              </div>
              <div class="activity-post-content">
                <p><strong>{post.data.title}</strong></p>
                {post.data.image && (
                  <img src={post.data.image} alt={post.data.title} class="activity-post-image" />
                )}
              </div>
            </article>
          ))}
        </div>
        <div style="text-align: center; margin-top: 2rem;">
          <a href="/aktuelt/" class="btn btn-outline">Se alle innlegg</a>
        </div>
      </div>
    </section>
  )}

  <!-- Latest Portfolio -->
  {portfolioItems.length > 0 && (
    <section class="section">
      <div class="container">
        <div class="section-title">
          <h2>Noen få arbeidsprøver</h2>
        </div>
        <div class="card-grid">
          {portfolioItems.map((item) => (
            <a href={`/portfolio/${item.id}/`} class="card" style="text-decoration: none; color: inherit;">
              {item.data.image && (
                <img src={item.data.image} alt={item.data.title} class="card-image" />
              )}
              <div class="card-body">
                <div class="card-meta">
                  {item.data.client && <span>{item.data.client}</span>}
                  {item.data.client && ' — '}
                  {new Date(item.data.date).toLocaleDateString('nb-NO', { year: 'numeric' })}
                </div>
                <h3>{item.data.title}</h3>
                <p>{item.data.description}</p>
              </div>
            </a>
          ))}
        </div>
        <div style="text-align: center; margin-top: 2rem;">
          <a href="/portfolio/" class="btn btn-outline">Se alle arbeidsprøver</a>
        </div>
      </div>
    </section>
  )}
</BaseLayout>
